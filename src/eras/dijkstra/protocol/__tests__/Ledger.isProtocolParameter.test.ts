// Cloned from the Conway era scaffold. The original fixtures validated a
// Babbage-era CLI protocol-params dump (protocolVersion major 7) against the
// Dijkstra guards — NOT real Dijkstra protocol-parameter data. No genuine
// Dijkstra protocol-param vectors exist yet.
// Re-enable with real Dijkstra protocol-parameter data once available.
test.todo("DijkstraProtocolParameters validation — no real Dijkstra param data available yet");
